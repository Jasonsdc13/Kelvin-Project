// The forecats today is 293 kelvin
const kelvin = 0;

// celsius is 273 degrees less than kelvin
var celsius = (kelvin - 273);

// We are conveting celsius to fahrenheit
var fahrenheit = celsius * (9/5) + 32;

// We are rounding fahrenheit to the lowest whole number
Math.floor(fahrenheit);

var temperature = fahrenheit;
console.log(`The temperature is ${temperature} degrees Fahrenheit.`);

// I'm converting celsius to newton in the following lines

// var newton = (celsius * (33/100));

// Math.floor(newton);

// var temperature = newton;
// console.log(`The temperature is ${temperature} degrees newton.`);
